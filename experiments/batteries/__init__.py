"""Evaluation batteries (stub package): scripted probes of Ledger calibration.

Each battery module exposes ``run(world, agent, cfg) -> dict[str, float]`` and
must report an identity-predictor baseline for every forecast metric.
"""
