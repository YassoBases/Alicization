# Full config: long experiment run — hours to overnight on a laptop.
# Per the scale-up path in docs/training.md: change one axis at a time; if
# reward collapses after a scale-up, check seq_len vs. episode structure and lr.

inherit: base.yaml

agent: {hidden_size: 384}
ppo: {num_envs: 32, total_steps: 10_000_000}
