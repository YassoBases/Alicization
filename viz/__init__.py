"""Visualization: live viewer, streamlit dashboard, and static plots.

Heavy deps (matplotlib, streamlit) are imported lazily inside functions so the
package imports cleanly in headless/test environments.
"""
